# Login/Register Authentication Flow Diagrams

## Registration Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         REGISTRATION FLOW                            │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Browser    │
│  Register.tsx│
└──────┬───────┘
       │ 1. User fills form
       │    - email
       │    - password
       │    - confirmPassword
       │    - role (INFLUENCER/COMPANY)
       │
       │ 2. Client validation
       │    - All fields required
       │    - Password >= 8 chars
       │    - Passwords match
       │
       ▼
┌──────────────┐
│ AuthContext  │
│   register() │
└──────┬───────┘
       │ 3. Call authService.register()
       │
       ▼
┌──────────────┐
│ AuthService  │
│  (Frontend)  │
└──────┬───────┘
       │ 4. POST /api/auth/register
       │    Body: { email, password, role }
       │
       ▼
┌──────────────┐
│  API Client  │
└──────┬───────┘
       │ 5. HTTP Request with headers
       │    Content-Type: application/json
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                        BACKEND                                    │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│AuthController│
│  register()  │
└──────┬───────┘
       │ 6. Validate RegisterDto
       │    - Email format
       │    - Password min length
       │    - Role enum
       │
       ▼
┌──────────────┐
│ AuthService  │
│  (Backend)   │
└──────┬───────┘
       │ 7. Check email uniqueness
       │    SELECT * FROM users WHERE email = ?
       │
       ├─ Email exists? → 409 Conflict Error
       │
       │ 8. Hash password
       │    bcrypt.hash(password, 10)
       │
       ▼
┌──────────────┐
│   Database   │
│ users table  │
└──────┬───────┘
       │ 9. INSERT INTO users
       │    (id, email, password, role, ...)
       │
       ▼
┌──────────────┐
│   Database   │
│profile table │
└──────┬───────┘
       │ 10. INSERT INTO influencer_profiles
       │     OR company_profiles
       │     (id, userId, ...)
       │
       ▼
┌──────────────┐
│ AuthService  │
│  (Backend)   │
└──────┬───────┘
       │ 11. Generate JWT
       │     jwt.sign({ sub: userId, email, role })
       │     Expiry: 7 days
       │
       │ 12. Fetch user with profile
       │     getUnifiedProfileData()
       │
       │ 13. Return { user, token }
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                        FRONTEND                                   │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│ AuthService  │
│  (Frontend)  │
└──────┬───────┘
       │ 14. Store token
       │     apiClient.setToken(token)
       │     localStorage.setItem('auth_token', token)
       │
       ▼
┌──────────────┐
│ AuthContext  │
└──────┬───────┘
       │ 15. Fetch profile
       │     GET /api/auth/me
       │
       │ 16. Set user state
       │     setUser(profile)
       │
       ▼
┌──────────────┐
│   Browser    │
│  Navigate to │
│  Dashboard   │
└──────────────┘
       │
       │ 17. Show success toast
       │     "Account created! 🎉"
       │
       ▼
    [DONE]
```


## Login Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                           LOGIN FLOW                                 │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Browser    │
│   Login.tsx  │
└──────┬───────┘
       │ 1. User enters credentials
       │    - email
       │    - password
       │
       │ 2. Client validation
       │    - Both fields required
       │
       ▼
┌──────────────┐
│ AuthContext  │
│    login()   │
└──────┬───────┘
       │ 3. Call authService.login()
       │
       ▼
┌──────────────┐
│ AuthService  │
│  (Frontend)  │
└──────┬───────┘
       │ 4. POST /api/auth/login
       │    Body: { email, password }
       │
       ▼
┌──────────────┐
│  API Client  │
└──────┬───────┘
       │ 5. HTTP Request
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                        BACKEND                                    │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│AuthController│
│   login()    │
└──────┬───────┘
       │ 6. Validate LoginDto
       │
       ▼
┌──────────────┐
│ AuthService  │
│  (Backend)   │
└──────┬───────┘
       │ 7. Find user by email
       │    SELECT * FROM users WHERE email = ?
       │
       ├─ Not found? → 401 Unauthorized
       │
       │ 8. Compare password
       │    bcrypt.compare(password, user.password)
       │
       ├─ Invalid? → 401 Unauthorized
       │
       │ 9. Generate JWT
       │    jwt.sign({ sub: userId, email, role })
       │
       │ 10. Fetch user with profile
       │     getUnifiedProfileData()
       │
       │ 11. Return { user, token }
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                        FRONTEND                                   │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│ AuthService  │
│  (Frontend)  │
└──────┬───────┘
       │ 12. Store token
       │     localStorage.setItem('auth_token', token)
       │
       ▼
┌──────────────┐
│ AuthContext  │
└──────┬───────┘
       │ 13. Fetch profile
       │     GET /api/auth/me
       │
       │ 14. Set user state
       │     setUser(profile)
       │
       ▼
┌──────────────┐
│   Browser    │
│  Navigate to │
│  Dashboard   │
└──────────────┘
       │
       │ 15. Show success toast
       │     "Welcome back!"
       │
       ▼
    [DONE]
```


## Auto-Login Flow (Page Refresh)

```
┌─────────────────────────────────────────────────────────────────────┐
│                      AUTO-LOGIN ON REFRESH                           │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Browser    │
│ Page Refresh │
└──────┬───────┘
       │ 1. App mounts
       │
       ▼
┌──────────────┐
│ AuthContext  │
│  useEffect() │
└──────┬───────┘
       │ 2. Check localStorage
       │    token = localStorage.getItem('auth_token')
       │
       ├─ No token? → Set loading=false, DONE
       │
       │ 3. Token found, fetch profile
       │    authService.getProfile()
       │
       ▼
┌──────────────┐
│ AuthService  │
│  (Frontend)  │
└──────┬───────┘
       │ 4. GET /api/auth/me
       │    Headers: Authorization: Bearer {token}
       │
       ▼
┌──────────────┐
│  API Client  │
└──────┬───────┘
       │ 5. HTTP Request with token
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                        BACKEND                                    │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│ JwtAuthGuard │
└──────┬───────┘
       │ 6. Extract token from header
       │
       │ 7. Verify JWT signature
       │    jwt.verify(token, JWT_SECRET)
       │
       ├─ Invalid? → 401 Unauthorized
       │
       │ 8. Decode payload
       │    { sub: userId, email, role }
       │
       │ 9. Attach to request
       │    request.user = payload
       │
       ▼
┌──────────────┐
│AuthController│
│     me()     │
└──────┬───────┘
       │ 10. Get userId from request.user.sub
       │
       ▼
┌──────────────┐
│ AuthService  │
│  (Backend)   │
└──────┬───────┘
       │ 11. Fetch user with profile
       │     getUserWithProfile(userId)
       │
       │ 12. Return user object
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│                        FRONTEND                                   │
└──────────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│ AuthContext  │
└──────┬───────┘
       │ 13. Set user state
       │     setUser(profile)
       │
       │ 14. Set loading=false
       │
       ▼
┌──────────────┐
│   Browser    │
│ User stays   │
│  logged in   │
└──────────────┘
       │
       ▼
    [DONE]

┌─────────────────────────────────────────────────────────────────────┐
│                      ERROR HANDLING                                  │
└─────────────────────────────────────────────────────────────────────┘

If 401 Unauthorized:
  → Clear token from localStorage
  → Set user = null
  → Redirect to login

If Network Error:
  → Keep token (resilient)
  → Show error message
  → User can retry
```


## Data Flow Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                    SYSTEM ARCHITECTURE                               │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                         FRONTEND LAYER                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────┐         ┌──────────────┐                         │
│  │  Login.tsx   │         │ Register.tsx │                         │
│  │              │         │              │                         │
│  │ - Form UI    │         │ - Form UI    │                         │
│  │ - Validation │         │ - Validation │                         │
│  │ - Submit     │         │ - Role Select│                         │
│  └──────┬───────┘         └──────┬───────┘                         │
│         │                        │                                  │
│         └────────────┬───────────┘                                  │
│                      │                                               │
│                      ▼                                               │
│            ┌──────────────────┐                                     │
│            │  AuthContext     │                                     │
│            │                  │                                     │
│            │ - user state     │                                     │
│            │ - login()        │                                     │
│            │ - register()     │                                     │
│            │ - logout()       │                                     │
│            │ - refreshProfile│                                     │
│            └────────┬─────────┘                                     │
│                     │                                                │
│                     ▼                                                │
│            ┌──────────────────┐                                     │
│            │  AuthService     │                                     │
│            │                  │                                     │
│            │ - login()        │                                     │
│            │ - register()     │                                     │
│            │ - getProfile()   │                                     │
│            │ - logout()       │                                     │
│            └────────┬─────────┘                                     │
│                     │                                                │
│                     ▼                                                │
│            ┌──────────────────┐                                     │
│            │   API Client     │                                     │
│            │                  │                                     │
│            │ - HTTP methods   │                                     │
│            │ - Token mgmt     │                                     │
│            │ - Error handling │                                     │
│            └────────┬─────────┘                                     │
│                     │                                                │
└─────────────────────┼────────────────────────────────────────────────┘
                      │
                      │ HTTP/JSON
                      │
┌─────────────────────┼────────────────────────────────────────────────┐
│                     │          BACKEND LAYER                         │
├─────────────────────┼────────────────────────────────────────────────┤
│                     ▼                                                │
│            ┌──────────────────┐                                     │
│            │ AuthController   │                                     │
│            │                  │                                     │
│            │ POST /register   │                                     │
│            │ POST /login      │                                     │
│            │ GET  /me         │◄──┐                                 │
│            │ PUT  /profile    │   │ JwtAuthGuard                    │
│            │ POST /logout     │◄──┘                                 │
│            └────────┬─────────┘                                     │
│                     │                                                │
│                     ▼                                                │
│            ┌──────────────────┐                                     │
│            │   AuthService    │                                     │
│            │                  │                                     │
│            │ - register()     │                                     │
│            │ - login()        │                                     │
│            │ - getCurrentUser │                                     │
│            │ - updateProfile  │                                     │
│            │ - generateToken  │                                     │
│            └────────┬─────────┘                                     │
│                     │                                                │
│                     ▼                                                │
│     ┌───────────────┴───────────────┐                              │
│     │                                │                              │
│     ▼                                ▼                              │
│ ┌─────────────┐            ┌─────────────────┐                    │
│ │   bcrypt    │            │  jsonwebtoken   │                    │
│ │             │            │                 │                    │
│ │ - hash()    │            │ - sign()        │                    │
│ │ - compare() │            │ - verify()      │                    │
│ └─────────────┘            └─────────────────┘                    │
│                                                                     │
└─────────────────────┬───────────────────────────────────────────────┘
                      │
                      │ TypeORM
                      │
┌─────────────────────┼───────────────────────────────────────────────┐
│                     │         DATABASE LAYER                        │
├─────────────────────┼───────────────────────────────────────────────┤
│                     ▼                                                │
│            ┌──────────────────┐                                     │
│            │  PostgreSQL DB   │                                     │
│            └────────┬─────────┘                                     │
│                     │                                                │
│         ┌───────────┼───────────┐                                   │
│         │           │           │                                   │
│         ▼           ▼           ▼                                   │
│   ┌─────────┐ ┌──────────┐ ┌──────────┐                           │
│   │  users  │ │influencer│ │ company  │                           │
│   │         │ │_profiles │ │_profiles │                           │
│   │ - id    │ │          │ │          │                           │
│   │ - email │ │ - userId │ │ - userId │                           │
│   │ - pass  │ │ - name   │ │ - name   │                           │
│   │ - role  │ │ - niche  │ │ - industry│                          │
│   │ - ...   │ │ - ...    │ │ - ...    │                           │
│   └─────────┘ └──────────┘ └──────────┘                           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

